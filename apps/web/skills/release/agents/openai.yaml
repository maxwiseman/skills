interface:
  display_name: Release Manager
  short_description: Prepare and publish versioned releases with changelog and git tags
  brand_color: "#f97316"

policy:
  allow_implicit_invocation: false

dependencies:
  tools:
    - type: cli
      value: git
      description: Required for tagging, pushing, and reading commit history
